name: Release

# Tag-only: publishing is irreversible, so this never runs on PRs or branch pushes.
on:
  push:
    tags:
      - 'v*'

permissions:
  contents: read
  id-token: write # OIDC: npm Trusted Publishing auth + provenance attestation

jobs:
  publish:
    name: Publish to npm
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          # Do NOT set registry-url here: it makes setup-node write an .npmrc
          # auth line with a placeholder NODE_AUTH_TOKEN, and npm then skips the
          # OIDC trusted-publisher exchange (masked E404). npmjs.org is the default.

      # npm OIDC Trusted Publishing requires npm >= 11.5.1 (Node 20 ships npm 10).
      # Upgrade BEFORE corepack so a corepack `npm` shim can't shadow it.
      - name: Upgrade npm (Trusted Publishing needs npm >= 11.5.1)
        run: |
          npm install -g npm@latest
          npm --version

      # `corepack enable` (no args) also shims `npm` and would shadow the upgrade.
      - name: Enable Corepack (Yarn)
        run: corepack enable yarn

      - name: Install dependencies
        run: yarn install --no-immutable # no standalone lockfile; CI forces immutable by default

      - name: Build
        run: yarn build

      # OIDC Trusted Publishing (configured on npmjs.com for GeniusVentures/hardhat-multichain):
      # no NPM_TOKEN — auth comes from the OIDC id-token; provenance is generated.
      - name: Publish (npm OIDC Trusted Publishing)
        env:
          NODE_AUTH_TOKEN: '' # any set token disables the OIDC exchange
        run: |
          echo "npm: $(npm --version)"
          echo "OIDC id-token URL present: ${ACTIONS_ID_TOKEN_REQUEST_URL:+YES}"
          echo "registry: $(npm config get registry)"
          npm publish --provenance --access public --tag latest
